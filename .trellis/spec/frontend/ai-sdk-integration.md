# AI SDK Frontend Integration — REMOVED FROM IRIS (2026-08-25)

> **Status: never shipped, now moot.** `@ai-sdk/react` was reference material
> only — no Iris component ever used it, and price extraction moved entirely to
> the external **argus** service (`POST /v1/extract-price`). There is no client
> AI surface to build against.

If chat/streaming features are ever added, consult git history for the prior
`useChat` / streaming notes, and add the dependency deliberately with a spec
review.
